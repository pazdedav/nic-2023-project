configuration:
  AZURE_BICEP_FILE_EXPANSION: true

# Configure binding options
binding:
  targetName:
    - ResourceName
    - name
  targetType:
    - ResourceType
    - type

input:
  pathIgnore:
  # Exclude all files
  - 'infra/**'

rule:
  include:
    - Azure.Policy.*